<#assign list1 = query(" select top 3 id,title,add_date,content,image from article where  is_pub=1 and title like '%维修%' and image<>'' order by add_date desc ")> 
		  
		  <#if list1??&&(list1?size>0)>
          <#list list1 as one>
	       <div class="ban_c"> <a href="/news_${one.id?c}.htm" target="_blank" title="${one.title}" class="_img"> <img src="http://news.21-sun.com/UserFiles/Image/${one.image}" alt="${one.title}"  /> </a> <a href="/info_${one.id?c}.htm" target="_blank" title="${one.title}" class="_t"> ${one.title} </a> </div>
          </#list>
          </#if>
		  
	
	